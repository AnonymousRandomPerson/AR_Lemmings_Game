using System;

namespace Lemmings.Entities.Blocks {
    /// <summary>
    /// The direction that a direction block will steer a lemming in.
    /// </summary>
    public enum Direction {
        Left,
        Right
    }
}


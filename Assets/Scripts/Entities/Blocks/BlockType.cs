using System;

namespace Lemmings.Entities.Blocks {
    /// <summary>
    /// Types of blocks that the player can place.
    /// </summary>
    public enum BlockType {
        LeftBlock,
        RightBlock,
        JumpPad,
        NumTypes
    }
}


using UnityEngine;

namespace Lemmings.Enums {

    /// <summary>
    /// Possible keys to store in Unity's PlayerPrefs.
    /// </summary>
    public enum PlayerPrefsKey {
        Path,
        Movement,
        Difficulty,
        Server
    }
}